% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_standard_design.R
\name{plot_standard_design}
\alias{plot_standard_design}
\title{Plot standard fieldbook experimental designs}
\usage{
plot_standard_design(
  data,
  factor = NA,
  fill = "plots",
  xlab = NULL,
  ylab = NULL,
  glab = NULL,
  text_size = NULL,
  wrap_width = NULL,
  font_family = "Open Sans",
  font_face = "plain"
)
}
\arguments{
\item{data}{A fieldbook data frame. It must contain at least \code{rows} and
\code{cols}.}

\item{factor}{Character scalar. Name of the column used to color the
experimental units. If missing, \code{"block"} is used when available;
otherwise, the third column of \code{data} is used.}

\item{fill}{Character vector. Names of one or more columns used as labels
inside each experimental unit. When \code{ntreat} is used, it is displayed as
\code{T1}, \code{T2}, etc.}

\item{xlab}{Character scalar. Title for the x axis. If \code{NULL}, \code{"Columns"}
is used.}

\item{ylab}{Character scalar. Title for the y axis. If \code{NULL}, \code{"Blocks"} is
shown for RCBD/DBCA only when each physical row corresponds to exactly one
block; otherwise \code{"Rows"} is used. The plotting coordinate always remains
the physical \code{rows} column.}

\item{glab}{Character scalar. Legend title. If \code{NULL}, \code{factor} is used.}

\item{text_size}{Optional positive numeric scalar indicating the plot-label
font size in typographic points (\code{pt}). If \code{NULL} or \code{NA}, a suitable
default is selected according to the number of label columns. The value is
converted internally to the unit expected by \code{ggplot2::geom_text()}.}

\item{wrap_width}{Optional positive integer indicating the approximate
maximum number of characters per line. If \code{NULL} or \code{NA}, the function
calculates it automatically from the field dimensions, font size and
number of label columns. Underscores are shown as spaces only in the
plotted label; the original fieldbook values are not modified.}

\item{font_family}{Character scalar. Font family used by the sketch.
Defaults to \code{"Open Sans"}. If the font cannot be verified through the
optional \code{systemfonts} package, \code{"sans"} is used as a fallback.}

\item{font_face}{Character scalar. Font face used in labels, axes and
legends. Defaults to \code{"plain"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot standard fieldbook sketches for regular experimental designs generated
in Tarpuy, including completely randomized designs (CRD/DCA) and randomized
complete block designs (RCBD/DBCA).
}
\details{
The function does not calculate or rearrange an experimental design. It uses
the existing \code{rows} and \code{cols} coordinates from the fieldbook, so the
physical layout and the zigzag order generated by Tarpuy are preserved.

All standard designs are plotted with \code{cols} on the x axis and \code{rows} on
the y axis. A DBCA/RCBD is therefore displayed according to its actual
physical coordinates rather than replacing the row coordinate with the
block number. When rows and blocks have a strict one-to-one relationship,
only the visible y-axis title changes to \code{"Blocks"}. The block can still be
selected as the color factor.

Automatic wrapping affects only the sketch. It does not change \code{entry},
\code{ntreat}, QR codes, factor levels or any other fieldbook value.
}
\examples{
\dontrun{

plot_standard_design(
  data = fieldbook,
  factor = "geno",
  fill = c("plots", "entry"),
  text_size = 9,
  font_family = "Open Sans",
  font_face = "plain"
)

}
}
