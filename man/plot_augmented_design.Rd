% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_augmented_design.R
\name{plot_augmented_design}
\alias{plot_augmented_design}
\title{Plot augmented fieldbook design}
\usage{
plot_augmented_design(
  data,
  factor = NA,
  fill = "plots",
  xlab = NULL,
  ylab = NULL,
  glab = NULL,
  text_size = NULL,
  wrap_width = NULL,
  font_family = "Open Sans",
  font_face = "plain"
)
}
\arguments{
\item{data}{Fieldbook data frame from an augmented design.}

\item{factor}{Character scalar. Column used to color experimental units.
If missing, \code{"type"} is used.}

\item{fill}{Character vector. Column or columns used as labels inside each
experimental unit. Default is \code{"plots"}. When \code{ntreat} is selected, it is
displayed as \code{T1}, \code{T2}, etc.}

\item{xlab}{Character scalar. Optional x axis title. If \code{NULL}, \code{"Columns"}
is used.}

\item{ylab}{Character scalar. Optional y axis title. If \code{NULL}, the title is
\code{"Blocks"} when every physical row corresponds to exactly one statistical
block; otherwise \code{"Rows"} is used.}

\item{glab}{Character scalar. Optional legend title. If \code{NULL}, the selected
color factor is used.}

\item{text_size}{Optional positive numeric scalar indicating the plot-label
font size in typographic points (\code{pt}). If \code{NULL} or \code{NA}, a suitable
default is selected according to the number of label columns. The value is
converted internally to the unit expected by \code{ggplot2::geom_text()}.}

\item{wrap_width}{Optional positive integer indicating the approximate
maximum number of characters per line. If \code{NULL} or \code{NA}, the function
calculates it automatically from the field dimensions, font size and
number of label columns. Underscores are displayed as spaces only in the
sketch; the original fieldbook values are not modified.}

\item{font_family}{Character scalar. Font family used in the sketch.
Defaults to \code{"Open Sans"}. If the font cannot be verified through the
optional \code{systemfonts} package, \code{"sans"} is used as a fallback.}

\item{font_face}{Character scalar. Font face used in labels, axes and
legends. Defaults to \code{"plain"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot fieldbook sketches for augmented experimental designs generated by
\code{design_augmented()}.
}
\details{
The function always uses the physical \code{rows} and \code{cols} coordinates stored in
the fieldbook. Therefore, custom dimensions and zigzag layouts are preserved.
The \code{block} column never replaces \code{rows} as a plotting coordinate. When each
row represents exactly one block, only the visible y-axis title changes from
\code{"Rows"} to \code{"Blocks"}. The statistical block remains available as a color
factor and, when a block occupies a complete rectangular region, its external
border is highlighted.

Empty experimental units are represented by the level \code{"empty"}. When
\code{factor = "type"}, checks, test entries and empty plots receive stable
colors. Other factor columns use the regular TARPUY color palette.

Automatic wrapping and formatting affect only the displayed text. They do
not modify \code{entry}, \code{plots}, \code{ntreat}, QR codes or any other fieldbook value.
}
\examples{
\dontrun{

plot_augmented_design(
  data = fieldbook,
  factor = "type",
  fill = c("plots", "entry"),
  text_size = 9,
  font_family = "Open Sans",
  font_face = "plain"
)

}
}
