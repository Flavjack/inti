% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_split_rcbd_design.R
\name{plot_split_rcbd_design}
\alias{plot_split_rcbd_design}
\title{Plot Splitplot-RCBD fieldbook design}
\usage{
plot_split_rcbd_design(
  data,
  factor = NA,
  fill = "plots",
  xlab = NULL,
  ylab = NULL,
  glab = NULL,
  text_size = NULL,
  wrap_width = NULL,
  font_family = "Open Sans",
  font_face = "plain"
)
}
\arguments{
\item{data}{Fieldbook data frame from \code{design_split_rcbd()}.}

\item{factor}{Character scalar. Column used to color experimental units.
If missing, \code{"wp_sp"} is used when available; otherwise, \code{"ntreat"} is
used.}

\item{fill}{Character vector. Column or columns used as labels inside each
experimental unit. Default is \code{"plots"}.}

\item{xlab}{Character scalar. Optional x-axis title. If \code{NULL},
\code{"Whole plots"} is used.}

\item{ylab}{Character scalar. Optional y-axis title. If \code{NULL},
\code{"Subplots"} is used when rows can be interpreted consistently as
subplot positions within blocks; otherwise, \code{"Rows"} is used.}

\item{glab}{Character scalar. Optional legend title.}

\item{text_size}{Optional positive numeric scalar indicating the plot-label
font size in typographic points (\code{pt}). If \code{NULL} or \code{NA}, a suitable
default is selected according to the number of label columns. The value is
converted internally to the unit expected by \code{ggplot2::geom_text()}.}

\item{wrap_width}{Optional positive integer indicating the approximate
maximum number of characters per line. If \code{NULL} or \code{NA}, the function
calculates it automatically from the dimensions of each block, font size
and number of label columns. Underscores are displayed as spaces only in
the sketch; the fieldbook values are not modified.}

\item{font_family}{Character scalar. Font family used in the sketch.
Defaults to \code{"Open Sans"}. If the font cannot be verified through the
optional \code{systemfonts} package, \code{"sans"} is used as a fallback.}

\item{font_face}{Character scalar. Font face used in labels, axes, facet
titles and legends. Defaults to \code{"plain"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot fieldbook sketches for Splitplot-RCBD experimental designs generated
by \code{design_split_rcbd()}.
}
\details{
The function does not recalculate or rearrange the experimental design. It
uses the existing \code{rows} and \code{cols} coordinates from the fieldbook, so the
physical arrangement and zigzag order generated by Tarpuy are preserved.

Every experimental unit is positioned with \code{cols} on the x axis and \code{rows}
on the y axis. The \code{block} column is used only to identify the block panels;
it never replaces the physical row coordinate.

Within each block, \code{cols} represents whole-plot positions. The visible
y-axis tick labels are derived from the rank of the actual \code{rows} values
inside each block, so the panels can display subplot positions while the
geometry continues to use the original fieldbook coordinates. A black
external border identifies each whole plot.

Automatic wrapping affects only the displayed labels. It does not change
\code{plots}, \code{ntreat}, treatment factors, QR codes, rows, columns or any other
fieldbook value.
}
\examples{
\dontrun{

plot_split_rcbd_design(
  data = fieldbook,
  factor = "wp_sp",
  fill = c("plots", "ntreat"),
  text_size = 9,
  font_family = "Open Sans",
  font_face = "plain"
)

}
}
